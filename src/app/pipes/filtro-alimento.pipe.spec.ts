import { FiltroAlimentoPipe } from './filtro-alimento.pipe';

describe('FiltroAlimentoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAlimentoPipe();
    expect(pipe).toBeTruthy();
  });
});
